doNext:
- Editing only the name of a list does not work

Features:

- clear formular when added successfully
- make URL actually a link and prepend a http:// if not there
- passwordlist disabled_by to check if disabled by an admin
- possibility to disable user completely from passwordsmodule
