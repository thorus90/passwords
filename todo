doNext:
- Editing only the name of a list does not work
- when giving wrong password crypt.js gives back malformed data because when decrypting with first wrong password malformed data is given to second decryption
- generate drop down out of type list
- The Password has beend saved0
- when delete a password remove layout

Features:

- passwordlist disabled_by to check if disabled by an admin
- possibility to disable user completely from passwordsmodule
